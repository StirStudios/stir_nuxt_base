{
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    "moduleResolution": "Node",
    "paths": {
      "@stir/base/*": ["./*"], // Alias for everything in the layer
      "@/*": ["*"], // Root project aliases
      "~/*": ["*"] // Root project aliases
    }
  }
}
